#include <stdio.h>
#include <unistd.h>
#include "main.h"
/**
 * swap_int - Entry point
 * @a: pointer a change value
 * @b: pointer b change value
 * Return: void
 */
int _strlen(char *s)
{
printf("%lu\n",sizeof(char *s));
}
