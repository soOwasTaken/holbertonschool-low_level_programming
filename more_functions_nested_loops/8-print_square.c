#include <stdio.h>
#include <unistd.h>
#include "main.h"
/**
* print_square - call point
* @size: int required
* Return: a square of @size
*/
void print_square(int size)
{
int a, b;
if (n <= 0)
	putchar('\n');
for (a = 0; a < n; a++)
{
	for (b = 0; b < n; b++)
	{
	_putchar('#');
	}
_putchar('\n');
}
}
