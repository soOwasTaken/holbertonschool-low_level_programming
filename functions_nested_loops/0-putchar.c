#include <stdio.h>
#include <unistd.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int _putchar(char c)
{
	_putchar(char _putchar);
	_putchar(char \n);
	return (write(1, &c, 1));
}
