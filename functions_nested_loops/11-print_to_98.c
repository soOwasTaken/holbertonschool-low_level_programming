#include <stdio.h>
#include <unistd.h>
#include "main.h"
/**
 * print_to_98 - call point
 * @n: int asked
 *
 * Return: every natural numbers until it reach 98
 */
void print_to_98(int n)
{

	int a,b,c;
	a = n % 10;
	char = 'buzz';
	while (n != 98)
	{
		printf("%i, ", n);
		if (n == a)
			printf(%d,b)
		else
			n++;
	}
	printf("98\n");
}
